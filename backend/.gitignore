# Backend-local ignores (root .gitignore covers most of these too).
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
htmlcov/
data/
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example
# Self-signed TLS material for the local HTTPS dev listener (docs/ops/backend-https-443.md).
# Dev-only certs — never committed; regenerated locally via openssl.
certs/
# Generated bias-test artifact (regenerated by the `bias-test` CI job per run,
# attached to each release per bias-testing.md §5 / [CRED_4DE05BB1] §7 Gate C).
bias-report.json
