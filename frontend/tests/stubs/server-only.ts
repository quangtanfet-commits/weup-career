// Test stub for the `server-only` package, which throws when imported outside
// the Next.js server runtime. In Vitest we want to unit-test RSC modules with a
// mocked global fetch, so this no-op stands in for it.
export {};
